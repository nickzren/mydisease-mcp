name: mydisease-mcp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - mcp>=1.0.0
    - httpx>=0.27.0
    - pydantic>=2.0.0
    - pytest>=8.0.0
    - pytest-asyncio>=0.23.0
    - pytest-cov>=5.0.0
    - pytest-mock>=3.12.0
    - -e .